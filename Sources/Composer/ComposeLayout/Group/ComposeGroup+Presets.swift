//
//  ComposeGroup+Presets.swift
//  Composer
//
//  Created by 이창준 on 9/28/23.
//

import Foundation

extension ComposeGroup {
    
    // Full (inset, innerGroup)
    
    // singleFullList (height, inset, innerGroup)
    
    // grid (height, numberOfItems, spacing, insets, innerGroup)
    
    // flow (height, numberOfItems, spacing, insets, innerGroup)
    
    // list (width, numberOfItems, spacing, insets, innerGroup)
    
    // custom (width, height, direction, numberOfItems, spacing, inset, innterGroup)
    
}
